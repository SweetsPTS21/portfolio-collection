# chi-portfolio — Vũ Huyền Chi

Standalone React portfolio app built from `docs/portfolio.md`.

Style: `fashion-editorial` with a high-end magazine layout, asymmetrical spreads, cherry accent, large Vietnamese-safe serif display typography, and self-created SVG assets under `src/assets/fashion-editorial/`.

Run:

```bash
npm install
npm run dev
```
